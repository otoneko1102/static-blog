# Example environment variables for local development
# Copy this to `.env` and adjust as needed.

# Base URL of the site (used by remark-link-card to resolve local `/...` links for OGP fetching).
# Must match SITE_URL in src/consts.ts
SITE=https://blog.montblank.fun
