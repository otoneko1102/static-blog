---
import { jst } from "../utils/jst";

interface Props {
  date: Date;
}

const { date } = Astro.props;

const d = jst(date);
const formatted = d.format("YYYY/MM/DD");
const datetimeTokyo = d.format("YYYY-MM-DDTHH:mm:ssZ");
---

<time datetime={datetimeTokyo}>{formatted}</time>
